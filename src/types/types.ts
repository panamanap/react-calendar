export interface CurrentDate {
    day: number;
    month: number;
    year: number;
}
export interface OrdersState {
    id: string;
    name: string;
}

export interface UsersState {
    email: string,
    name: string,
    price: number,
}
